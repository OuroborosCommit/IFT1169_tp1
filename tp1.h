/*Etapes : 
Lire le fichier
Lire les parametres
Lire le fichier paragam.txt
Vu qu'on devra modifier la lecture des donnees dans tp2, on fait un programe pour chaque (modulaire)

On doit faire un makefile pour compiler le projet
On doit faire un programme tp1.h
on va faire un programme grader.cpp
on va faire un fichier de test qui va sappeler test_csv.cpp
on va faire un fichier test_config.cpp */

#ifndef TP1_HEADER_H ; 
#define TP1_HEADER_H ; 
#include <iostream> 
using namespace std;

struct Params {
	
	input("input.txt"), output("output.txt"), print(false)
	std:string input;
	std:string output;
	bool print;
	
}

	Params read_params(int argc, char* argv[]){
		
		if (std::string(argv[i]== std::string("-i") {
			
			
		}
		/*
		 --input (-i) file_name
		 --output (-o) file_name
		 --print (-p)
		*/
		
		Params p ; 
		return p ; 
	}
	

	Students read_csv (std::string file_name){
		return ; 
	}
	config read_config(std::string file_name){}
	
	//void compute_grade(Students& s){}
	// void grade_to_csv(std::string file_name, const Students&){}

	
	void pretty_print (const Students& s){
		
		/* 
		*    Pretty Print a CSV file
		*
		*  .---------------------------.
		*  | login    | Name  | ....   |
		*  |----------------------------
		*  | ducharje | Jesse | .......|
		*  |                           |
		*  |                           |
		*  '---------------------------'
		*/
	}
	
	bool lire_donnes() {
		
		return false; 
	}
	return 0 ; 
}

#endif